#include "bash_variables.h"
#include <stdlib.h>

node *head = NULL :

    void
    insertionAtLast(bash_variable variable)
{
    /* Allocating memory for new node*/
    node *link = (node *)malloc(sizeof(node));

    /* insert data to the new node*/
    node->
}